for f in *.mp3; do mv "$f" "${f// /}"; done;
for f in *.mp3; do mv "$f" "${f//\'/}"; done;
for f in *.mp3; do mv "$f" "${f//(/}"; done;
for f in *.mp3; do mv "$f" "${f//)/}"; done;
for f in *.mp3; do mv "$f" "${f//,/}"; done;